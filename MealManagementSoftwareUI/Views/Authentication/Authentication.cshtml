@model MealManagementSoftwareUiLayer.ViewModel.AuthenticationViewModel

@{
    ViewData["Title"] = "Login";
}
<style>
    .login-wrapper {
        padding: 30px;
    }
    .content-wrapper {
        width: auto;
        margin-left: 0!important;
        min-height: 450px;
        background-color: white;
    }
    .inner-div {
        margin: 0 auto;
        width: 500px;
        height: 350px;
        text-align: center;
        color: white;
        background: rgb(2,0,36);
        padding: 30px;
        background: #2b484c;
    }
    .help-block ul {
        list-style-type: none;
        
        text-align: left;
        padding: 0; 
    }
</style>
<div class="login-wrapper">
    @if (Model != null && Model.Message != null)
    {
    <div class="text-center"> 
        <div class="alert alert-success" role="alert">
            @Model.Message
        </div>
    </div>
    }
    <div class="inner-div">
        <h4>Sign in</h4>
        <form asp-action="Authentication" asp-route-returnUrl="@ViewData["ReturnUrl"]" onsubmit="submitForm()">
            <div class="help-block"> @Html.ValidationSummary(true)</div>

            <div class="form-group">
                <div class="form-row">
                    <label for="Firstname">Username</label>
                    <input type="email" class="form-control" id="Username" autocomplete="off" name="Username" placeholder="Username">
                    @Html.ValidationMessageFor(model => model.Username, String.Empty,
           new { @class = "help-block" })
                </div>
                <div class="form-row">
                    <label for="Lastname">Password</label>
                    <input type="password" class="form-control" id="Password" autocomplete="off" name="Password" placeholder="Password">
                    @Html.ValidationMessageFor(model => model.Password, String.Empty,
           new { @class = "help-block" })
                </div>
            </div>

            <button type="submit" id="LoginBtn" class="btn btn-primary" style="margin-top: 40px; width:100px">Sign in</button>
        </form>
    </div>
</div>


<script>
    function submitForm() {

        document.getElementById("RegisterBtn").disabled = true;
        return true;
    }
</script>
